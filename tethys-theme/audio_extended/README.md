# Theme Audio Assets

This directory is populated via S3. Run:

```bash
python scripts/download_media.py --groups theme-audio
```

The script recreates the exact WAV/MP3 structure needed for the WordPress
theme and leaves Git history clean.
