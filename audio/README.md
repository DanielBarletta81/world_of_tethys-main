# Audio Projects

All large REAPER sources live in S3 under `media/audio`.
Download them when needed:

```bash
python scripts/download_media.py --groups audio-projects
```

Only lightweight documentation stays in Git.
